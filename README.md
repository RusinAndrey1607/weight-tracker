# Weight Tracker

Минималистичное PWA для личного трекинга веса. Стек: React, TypeScript, Vite, Firebase.

## Разработка

```bash
cp .env.example .env
npm install
npm run dev
```

## Скрипты

- `npm run dev` — локальный сервер
- `npm run build` — production build
- `npm run lint` — ESLint
